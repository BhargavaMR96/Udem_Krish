"# Krish-Practice" 

For uploading the changes done to the folder please use below in command prompt

Note: Make sure the folder is changed int he command prompt to D:\GitHub\Udem-Krish

git status

git add .

git commit -m "initial commit"

git push -u origin main

Any changes made directly on github or want to download the recent version gtom github use :

git pull origin main
